# harnex, checked by the oracle harnex ships.
#
# Only the validators that have a subject here are enabled: this repo declares
# rules, settings, and one skill, and carries no sub-agents or output styles
# yet — their sections stay on so the first one added is checked from its first
# commit rather than whenever someone remembers to widen this file.
#
# The one skill is deliberately in no `[[kinds]]`: a kind exists so the sweep
# can classify a corpus, and one manually-invoked artifact is not one. Declare
# it when there is a second.
#
# policy and codegen stay absent: this repo runs neither loop on itself, and
# `harnex check` reporting them skipped with a reason is the honest answer.
# lifecycle and telemetry are ON (below): the promotion/retirement loop this
# repo ships was field-tested only by its consumers until 2026-08 — the loop's
# own friction has to be findable here, on the corpus that builds it.

[meta]
harnex_version = ">=0.8, <0.9"

# What the retirement sweep walks. Without a declared kind the sweep runs
# over nothing and answers success — an enabled loop that processes nothing
# is the uncomputed-control shape wearing a config file. The rules corpus is
# the artifact class this repo actually accumulates.
[[kinds]]
name = "rule"
glob = ".claude/rules/*.md"
foundation = false

[[kinds]]
name = "constitution"
glob = ".claude/rules/constitution.md"
foundation = true

# The rules here name the module that owns each fact, which is the whole of
# Article IX, and a name is only worth more than prose while it still resolves.
# `harnex check` reads every `CLAUDE.md` the project owns and every surface it
# validates for shape, and holds every marked claim to the tree; the grammar is spelled once, in
# `plugins/harnex/templates/common/harness.toml`, and not again here.
#
# The subject is real: this is the check a generated harness now ships, and a
# rule pointing at a module that was renamed is exactly the drift the retirement
# sweep looks for by hand. The marker is what keeps it out of prose — a bare
# backtick path is documentation, and a host and port share the shape of a file
# and a line.
[evidence]
default_provenance = "internal"

[[evidence.verifiers]]
provenance = "internal"
strategy = "file-path-line"

[validate.rules]
# The Claude Code memory budget applies to what loads every turn. Every other
# rule here is path-scoped and costs context only on the files it governs, so
# `max_scoped_lines` stays unset — a cohesive long rule is not a defect.
max_lines = 200
always_loaded_slugs = ["constitution", "making-changes"]
require_governs = true

[validate.skills]

[validate.agents]

[validate.output_styles]

[session]
roots = ["~/.claude/projects"]

# The loop this repo ships, run on this repo. Thresholds keep their defaults
# until the ledger is large enough to have an opinion (same reasoning as the
# scaffold template). The observation and decision ledgers are tracked in git
# (repository memory); telemetry and session baselines stay machine-local
# (.gitignore holds the split).
[lifecycle]

# A rule is cited by file name — an evidence claim on its path, the
# documentation map, another rule's prose — and never by bare slug: `check`
# is a slug and an English word, and matched bare it names most of the tree.
# The ledgers under .harness/ are records of past measurements, not readers
# of a rule.
[[lifecycle.consumer_detectors]]
kind = "rule"
strategy = "grep"
pattern = "{slug}.md"
exclude_globs = [".claude/rules/{slug}.md", ".harness/**"]

# The always-loaded pair is load-bearing by construction; retiring either is
# a governance decision, not a sweep verdict. The constitution is carved out
# by its foundation kind above; this holds the other one.
[retirement.exempt]
slugs = ["making-changes"]

# Empty kinds on purpose: an event schema is declared when there is something
# to count (template rationale). Nothing records invocations of this project's
# one kind — a rule is loaded, not invoked — so no `[[kinds]] invocation_kind`
# is declared and `lifecycle retire` reports every rule `unmeasured` on the
# silence axis, which is the honest answer rather than a presumed silence.
# The section exists because the sweep reads the ledger regardless.
[telemetry]
storage_dir = ".harness/telemetry"
