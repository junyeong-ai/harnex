# <PROJECT_NAME> (AI operating context)

<!-- README.md is the only human-facing surface. This file and everything
     under .claude/ is consumed directly by Claude Code. Keep under 200 lines. -->

## Where things live

(add your directory map — what each top-level directory contains)

## Build & test

```bash
(add your build / test / dev commands)
```

## Invariants

(add invariants enforced by lint, test, or harness gate. Each line cites
the rule slug or test file.)

## What this project refuses to do

(add the negative space — what's deliberately out of scope)
