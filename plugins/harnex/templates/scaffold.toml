# Scaffold composition manifest — the single source of truth for what a
# generated harness contains.
#
# Three consumers read this file and no other statement of the composition
# exists: the skill emits these artifacts, the `plugin_scaffold_validates`
# test builds its fixture from them, and `harnex audit` reports which
# destinations a project already holds. A file list written a second time in
# prose is what let a scaffold fixture drift into omitting every hook while
# still reporting green (Constitution IX).
#
# Fields:
#   tier         "foundation" (language-agnostic) | "language" (needs a profile)
#   content      how the project's copy relates to this template — see below
#   template     path under `templates/`; `{lang}` resolves to the detected language
#   destination  project-relative path; `{lang}` resolves the same way
#   executable   the artifact is a script and is written 0o755
#
# `content.kind` is the discriminator every consumer switches on, because how
# an artifact is emitted, how its presence is tested, and what counts as drift
# are one decision rather than three:
#
#   copy     machinery — the project's copy must stay byte-identical, because
#            this template is the only statement of what it does.
#   seed     a starting point the project takes ownership of. Written once,
#            never held to the template afterwards: a project's governance is
#            its own, and tailoring it is the intended use, not drift.
#   managed  partitioned by `harnex-managed` sentinels — harnex owns what they
#            bound, the project owns everything outside them.
#   merge    one JSON fragment among several at a shared destination, unioned
#            at `key`. See UNION below for the rule and MERGE for why this kind
#            alone writes into an occupied destination.
#
# COLLISION — a destination that already exists is the project's file, and
# every kind except `merge` keeps it: emit nothing there and report it. This is
# the common case rather than the exceptional one — a repo with no `.claude/`
# usually still has a `CLAUDE.md`, because Claude Code reads one without any
# `.claude/` directory at all, and a repo with git hooks already has `hooks/`.
#
# `managed` is no exception, though it looks like one. Its partition governs a
# file harnex created and will regenerate; it is not standing to append to a
# file harnex did not write. Contributing a region to an incumbent would read as
# helpful on a `CLAUDE.md` that is three lines of notes and as presumptuous on a
# `constitution.md` the project already wrote — and a rule that needs the file's
# meaning to decide is not a rule.
#
# Emission is therefore idempotent: running a scaffold twice changes nothing
# the second time.
#
# MERGE — the one kind that writes into an occupied destination, because it
# never owns one. Several artifacts may name the same key (`hooks` and
# `permissions.allow` are each claimed by both tiers) and the contribution is a
# UNION, never a replacement: the language tier adds its PostToolUse entry
# beside the foundation's SessionStart and Stop rather than erasing them.
# Presence is the fragment landing at its key, not the shared destination
# existing.
#
# UNION — recursive. Objects merge key-wise AT EVERY DEPTH, arrays gain the
# elements they lack, an empty slot takes the fragment whole. Depth is not a
# refinement of the rule but the rule itself: `hooks` is an object whose values
# are arrays, so a union that stopped at the merge key would replace
# `hooks.SessionStart` whole and drop an entry the project wrote, leaving a
# structurally valid file no validator objects to.
#
# Where the two shapes DISAGREE, that node is left exactly as it was and the
# collision is reported; every sibling key still merges, so one malformed event
# does not withhold the rest. An operator who spelled `"SessionStart": {…}` instead of `[{…}]`
# has one real hook in a malformed file; replacing it to land the fragment
# drops their hook and the result still validates. Keeping theirs costs only a
# contribution they can re-apply once the shape is fixed, and it is the
# direction `regenerate` already takes on a conflict.
#
# Arrays gain elements at the end rather than being sorted. Hook entries under
# one event run in order, and a merge that never owns its destination may not
# renumber what it found there. Appending is idempotent regardless — a second
# pass finds every element present.
#
# No contribution may be claimed twice and two fragments meeting at one location
# must contribute the same shape, so the result does not depend on which tier
# lands first.
#
# Adding a language means adding its `<lang>-dev` profile plus the three
# `{lang}` template files — never an edit here. The language set derives from
# `PermissionProfile::ALL`, which is what the drift test resolves `{lang}`
# against in both directions.

# ---------- foundation: emitted for every project ----------

[[artifact]]
tier = "foundation"
content = { kind = "merge", key = "permissions.deny" }
template = "common/permissions.deny.json"
destination = ".claude/settings.json"

[[artifact]]
tier = "foundation"
content = { kind = "merge", key = "permissions.allow" }
template = "common/permissions.allow.json"
destination = ".claude/settings.json"

[[artifact]]
tier = "foundation"
content = { kind = "merge", key = "hooks" }
template = "common/hooks.json"
destination = ".claude/settings.json"

[[artifact]]
tier = "foundation"
content = { kind = "managed" }
template = "common/CLAUDE.md"
destination = "CLAUDE.md"

[[artifact]]
tier = "foundation"
content = { kind = "managed" }
template = "common/rules/constitution.md"
destination = ".claude/rules/constitution.md"

[[artifact]]
tier = "foundation"
content = { kind = "seed" }
template = "common/rules/agent-conduct.md"
destination = ".claude/rules/agent-conduct.md"

[[artifact]]
tier = "foundation"
content = { kind = "seed" }
template = "common/rules/governance.md"
destination = ".claude/rules/governance.md"

[[artifact]]
tier = "foundation"
content = { kind = "seed" }
template = "common/rules/artifact-lifecycle.md"
destination = ".claude/rules/artifact-lifecycle.md"

[[artifact]]
tier = "foundation"
content = { kind = "seed" }
template = "common/harness.toml"
destination = "harness.toml"

[[artifact]]
tier = "foundation"
content = { kind = "seed" }
template = "common/skills/harness-curate/SKILL.md"
destination = ".claude/skills/harness-curate/SKILL.md"

[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/_runner.sh"
destination = "hooks/_runner.sh"
executable = true

[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/_stop_runner.sh"
destination = "hooks/_stop_runner.sh"
executable = true

[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/session-start.sh"
destination = "hooks/session-start.sh"
executable = true

[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/check-on-stop.sh"
destination = "hooks/check-on-stop.sh"
executable = true

[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/git-hooks/pre-commit"
destination = "hooks/pre-commit"
executable = true

# Fail-open in three places — no binary, no `[validate.commit_msg]` section,
# and `HARNEX_SKIP_COMMITMSG` — so a project that has not configured trailers
# is never blocked by the hook that would enforce them.
[[artifact]]
tier = "foundation"
content = { kind = "copy" }
template = "common/git-hooks/commit-msg"
destination = "hooks/commit-msg"
executable = true

# ---------- language: emitted once per detected stack ----------
#
# Detection answers with a SET, not a winner. A lockfile is evidence that a
# stack is present, never evidence that it is the only one, and a repository
# with two of them has two — one measured here holds 17,085 `.py` files beside
# 3,433 `.ts`, and picking by row order would have handed it a formatter that
# silently skips 83% of its source. Every artifact below therefore resolves
# per language: `permissions.allow` and `hooks.PostToolUse` union across the
# matched set, and the conventions rule and formatter carry `{lang}` in their
# destination so two stacks never collide.
#
# Each `post-format-{lang}.sh` dispatches on the file extension and exits 0 on
# anything it does not own, so several of them coexist as separate PostToolUse
# entries with no arbitration between them.

[[artifact]]
tier = "language"
content = { kind = "merge", key = "permissions.allow" }
template = "{lang}/permissions.allow.json"
destination = ".claude/settings.json"

[[artifact]]
tier = "language"
content = { kind = "merge", key = "hooks.PostToolUse" }
template = "{lang}/hooks.format.json"
destination = ".claude/settings.json"

[[artifact]]
tier = "language"
content = { kind = "copy" }
template = "{lang}/post-format.sh"
destination = "hooks/post-format-{lang}.sh"
executable = true

[[artifact]]
tier = "language"
content = { kind = "seed" }
template = "{lang}/rules/{lang}-conventions.md"
destination = ".claude/rules/{lang}-conventions.md"
