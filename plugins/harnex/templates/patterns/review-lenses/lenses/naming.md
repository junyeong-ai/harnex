---
id: naming
applies_to: [code, design, spec, plan]
anchors:
  - constitution
  # On install, if the `naming-decisions` rule is present, add it here.
---

# Naming

Do new names match the project's recorded vocabulary?

- Follow the `naming-decisions` rule when it is installed; otherwise defer
  to the dominant convention already in the tree.
- The same concept uses the same term project-wide — flag a synonym
  introduced for an existing concept.
