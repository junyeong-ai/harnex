---
# <!-- harnex-fill: schedule the first tick — when: YYYY-MM-DD and
#      produces: the record path, e.g. .harness/records/<year>-q<n>-curate.md -->
cadence: quarterly
owner: harness
prompt: |
  Run /harness-curate and write its result to the path in produces:. The
  skill owns the procedure and the discipline; this routine owns the
  cadence and the record. Add the one pass no ledger reaches: for every
  rule line that corrects a perceived model or tool shortcoming, check
  whether the shortcoming still holds under the current model and
  toolchain — capability moves faster than the prose written against it,
  and nothing else in the harness looks at that class.
---

# Record
