{"tag":"symbol-boundary-is-language-dependent","normalized_text":"the symbol boundary is the c family identifier rule, so a name spelled with any other character has a proper prefix that resolves against it: check-types answers to a file spelling only check-types-and-lint, and the same holds for a css custom property and a ruby predicate","decision":"rejected","decision_text":"Rejected as a defect and stated as a limit. Widening the set was tried, released in 0.8.1, and withdrawn in the same week because it cost more than it bought. A character in the set does two things: it refuses a match abutted by it, and it removes that occurrence from the count. The second turns two readings into one and a violation into a pass, so `s.cached?` stopped counting as a use of cached and `!READY` as a use of READY — silent false passes on ordinary code. It also refused correct citations loudly: ?Sized, println!, wire_enum!, foo-1. One case bought, five sold. There is no safe direction to widen in because both mistakes are silent, and no language-agnostic set exists — the same character is part of a name in one language and an operator in the next. The grammar already carries the answer and the unresolved hint already gives it: cite the declaration as the file spells it. `check-types-and-lint:` and `def valid?` each name one place.","timestamp":"2026-09-04T08:45:31.953129Z"}
