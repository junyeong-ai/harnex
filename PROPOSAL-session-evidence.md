# 작업 계획: 하네스 개선 플라이휠

**상태:** 계획, 미구현. 2026-08-26 v1 제안서와 v2 재측정본을 대체한다.
**측정:** 2026-08-26, 로컬 Claude Code 코퍼스. 수치는 모두 재유도했다.
**작업이 착지하면 이 파일은 삭제한다** — 내용은 `crates/harness-core/src/session/` 모듈 doc,
`.claude/rules/session.md`, `plugins/harnex/reference/measurement.md` 로 간다.

---

## 0. 목적

> 사용자가 **내가 AI를 제대로 쓰고 있는지**를 확인하고, 수치로 보고, 필요하면 시각화로 이해하고,
> 그 결과로 하네스를 고치고 불필요한 것을 지우고, 다시 측정해서 나아졌는지 본다.

**관측의 주어는 하네스가 아니라 지시다.** 하네스가 몇 번 발동했는지는 그 자체로 가치가 없다 —
사용자가 무엇을 어떻게 지시했고, 어디서 끊고 개입했고, 무엇을 반복했고, 어디서 되돌아갔는지가
캐낼 대상이고, 하네스 지표는 그 원인 후보로서만 의미가 있다. 이 계획의 이전 판본은 순서가
뒤집혀 있었다.

이건 리포트가 아니라 **플라이휠**이다. 다섯 정거장이고, 각각 주인이 있어야 한다.

| 정거장 | 하는 일 | 주인 | 오늘 |
|---|---|---|---|
| **관측** | 사실을 센다, 인용을 붙여서 | `harness session` (신규 오라클) | **없음** |
| **진단** | 무슨 일이 있었는지 읽고 이름 붙인다 | `/harnex measure` (신규 스킬 모드) | **없음** |
| **처방** | 무엇을 바꿀지 정한다 | `enforced-vs-advisory` · `keep-soften-cut` | 지식은 있으나 증거가 없음 |
| **적용 (추가)** | 하네스에 더한다 | `/harnex extend` | 있음 |
| **적용 (제거)** | 하네스에서 뺀다 | **`/harnex retire` (신규 스킬 모드)** | **없음** |
| **재측정** | 나아졌는지 본다 | `harness session baseline diff` + 예약 실행 | **없음** |

여섯 칸 중 넷이 비어 있다. 그리고 그중 하나는 단순한 공백이 아니라 구조적 결함이다.

---

## 1. 오늘 harnex는 더할 수만 있고 뺄 수 없다

이게 이번 개정의 핵심 발견이다.

- `plugins/harnex/SKILL.md:207` — `## Mode: extend (brownfield, additive — closed verb menu)`.
  동사 메뉴 어디에도 제거가 없다.
- `crates/harness-core/src/audit/mod.rs` — *"Never modify any file. Findings only."*
- `lifecycle`의 `RetirementSweeper`는 *"aggregate retirement verdicts"* 를 반환한다. 판정만
  내놓고, 그 판정에 따라 프로젝트 파일을 건드리는 것은 아무것도 없다. 결정 원장의 `Demoted`는
  *패턴*의 강등이지 생성된 산출물의 제거가 아니다.
- `keep-soften-cut`의 CUT은 *"emit nothing"* — **생성 시점 필터**다. 이미 프로젝트에 놓인 것을
  빼는 동작이 아니다. 단어를 재사용하면 안 된다.

그래서 지금 플라이휠은 래칫이다. 한 방향으로만 돈다. §3의 관측 — 7,349번 발동해 **0번** 막은
Stop 게이트, **95 KB짜리 룰이 64번** 로드된 것 — 은 바로 그 래칫의 결과물이다. 측정이 만들어 내는
가장 값진 산출물은 *지워도 된다는 허가*인데, harnex에는 그 허가가 착지할 표면이 없다.

**따라서 이 계획의 산출물은 센서 하나가 아니라 둘이다: 센서와, 제거 경로.**

---

## 2. AI-first / AI-native가 구체적으로 바꾸는 것

"AI를 통해 확인하고 딥다이브한다"는 요구는 아키텍처를 네 군데에서 바꾼다. 이전 계획은
오라클 중심(바이너리 + CLI + 봉투)이었고 스킬은 얇은 판정 계층이었다. 뒤집는다.

### 2.1 진입점은 CLI가 아니라 대화다

사용자는 `harness session facts --since 30d`를 치지 않는다. **"내가 AI 제대로 쓰고 있어?"**
라고 말하고, 스킬이 오라클을 몰아친다. CLI는 스킬과 CI를 위한 표면이지 사람을 위한 표면이 아니다.
따라서 1차 산출물은 `SKILL.md`의 모드이고, CLI는 그것을 뒷받침하는 계약이다.

### 2.2 딥다이브 = AI가 원문 구간을 읽는다. 그러려면 모든 사실이 인용을 달아야 한다

오라클이 내는 모든 사실은 **인용**을 달고 나온다: `(세션 파일, 레코드 uuid, 타임스탬프)`.
그래야 (i) 스킬이 그 구간으로 들어가 실제 턴을 읽고 딥다이브하며, (ii) 사람이 그 숫자를 직접
검증한다.

이건 새 기계가 아니라 **`harness-core::evidence`의 재사용**이다. evidence는 이미 "주장은 출처를
가리켜야 한다"를 위해 존재하고 `file-path-line` / `context7` / `fetched-url` / `memory-only`
전략을 갖고 있다. 여기에 `session-record` 전략을 하나 더한다.

> **규칙: 진단 리포트의 모든 사실 문장은 `harness evidence` 검증을 통과해야 한다.**
> 인용 없는 문장은 관찰이 아니라 서술이고, 리포트에서 그렇게 표시된다.

AI가 만든 진단의 고유 위험은 그럴듯한 소설이다. 이 저장소는 이미 그 위험에 대한 답을 갖고 있었고,
지금까지 문서 주장에만 겨눠져 있었을 뿐이다.

### 2.3 시각화는 Artifact다. 단, 척추는 고정한다

스킬이 리포트를 아티팩트로 발행한다. 다만 플라이휠이 돌려면 **회차 간 비교가 가능해야** 하므로:

- **고정:** 섹션 척추, 지표 이름과 순서, 재실행 시 최상단의 baseline 델타 블록
- **자유:** 시각 처리, 산문, 어떤 그림을 그릴지

지표 이름이 회차마다 달라지면 델타를 못 낸다. 디자인이 회차마다 달라지는 건 아무 문제가 없다.

### 2.4 재측정이 스스로 돌아온다

예약 태스크가 `harness session baseline diff`를 돌리고, 델타가 임계를 넘을 때만 리포트를 연다.
사람이 기억해야 도는 플라이휠은 두 바퀴째에 멈춘다.

---

## 3. 사용자가 보는 것

### 3.1 다섯 개의 질문 — 이 도구가 캐내는 것

| 질문 | 캐내는 것 | 접지 신호 (결정 가능) |
|---|---|---|
| **어떻게 지시했나** | 모호한가 · 장황한가 · 너무 단순한가 | 직후 되물음, 자기 재첨부율, 구체 앵커 유무 |
| **어디서 개입했나** | 중단시키고 무엇을 시켰나, 그 개입이 값어치가 있었나 | 중단 레코드 → 직전 도구 → 직후 지시 삼중항 |
| **무엇을 반복했나** | 같은 지시를 왜 또 했나 | 세션 내 재진술 vs 세션 간 반복 |
| **어디서 되돌아갔나** | 끝났다고 한 것이 안 끝났나 | 커밋 후 다음 커밋 전 재편집 |
| **하네스가 무슨 짓을 했나** | 위 넷의 원인 후보 | 거부 원장, 훅 비용, 룰 로드 |

앞의 넷이 주어이고, 다섯째가 설명이다.

### 3.1.1 프롬프트 품질을 채점하는 방법 — 루브릭을 띄우지 않는다

**모호함은 텍스트의 속성이 아니라 `텍스트 × 그 시점의 컨텍스트`의 속성이다.** 긴 공유 컨텍스트
뒤의 "진행해"는 최적의 프롬프트이고, 차갑게 시작하는 "진행해"는 아무것도 지시하지 않는다. 같은
문자열에 같은 점수를 매기는 루브릭은 그래서 틀린다. RuVerBench가 말한 판정자 노이즈 이전에,
채점 대상 자체가 잘못 정의된 것이다.

그러므로 채점은 **직후에 실제로 일어난 일에 접지한다.** 전부 결정 가능하다:

| 접지 신호 | 무엇을 뜻하나 | 실측 (120 세션) |
|---|---|---|
| 직후 되물음 (`AskUserQuestion` 또는 물음표 종결) | 명세가 부족했다 | **16.2%** (4.6% + 11.6%) |
| 직후 중단 개입 | 방향이 틀렸다 | 247건 |
| 세션 내 재진술 (다른 제출에서 같은 문단) | 지시가 컨텍스트에서 밀렸다 | 문단 문자의 **1.7%** · 문단 89개 |
| 커밋 후 다음 커밋 전 재편집 | 완료 선언이 성급했다 | **2,438건 / 824파일** |
| 세션 간 동일 문단 반복 | 상시 지시가 설치되지 않았다 | 문단 문자의 **85.3%** · 문단 156개 |

텍스트 특징은 접지 신호와 **교차될 때만** 의미를 갖는다:

- **길이** — p50 60 / p90 619 / p99 3,425자, 10자 이하 19.5%
- **구체 앵커** — 프롬프트의 토큰이 **저장소에 실재하거나 그 세션이 실제로 건드린 파일 집합에
  있는가**. 확장자 목록을 정규식에 박는 방식은 Constitution VII 위반(소스에 프로젝트 어휘)이고
  언어가 늘 때마다 깨지며, 존재하지 않는 파일명도 앵커로 센다. 실재 검증은 목록이 필요 없고,
  *없는 것을 언급한 프롬프트*를 앵커가 아니라 **추측**으로 분리해 낸다 — 그게 더 쓸모 있는 구분이다
- **자기 재첨부율** — 이 프롬프트 중 몇 %가 사용자 자신의 이전 프롬프트에서 그대로 온 텍스트인가
- **붙여넣기 블록 여부**

**"장황"의 정의를 데이터로 바꾼다.** 장황 = 길다가 아니라 **길이 대비 새 정보가 적다**.

측정 원시값은 유사도 점수가 아니라 **공백 정규화 후 정확히 일치하는 문단**이다. shingle Jaccard는
임의 상수(창 길이 · 보폭)를 요구하고 그 상수의 의미가 언어마다 달라지므로 — 한국어 12자는 한 구,
영어 12자는 두 단어 — 다국어 코퍼스에서 오탐 바닥을 갖는다. 정확 일치는 그 바닥이 0이고,
`min_block_chars`는 소스 상수가 아니라 `harness.toml` 파라미터다.

두 값은 별개이며 처방이 다르다:

| | 실측 (120 세션, 문단 ≥ 40자) | 뜻 | 처방 |
|---|---|---|---|
| **세션 내** 정확 재사용 | 968,936 / 1,385,244자 = **69.9%** | 한 세션 안에서 같은 문단을 다시 붙임 | 컨텍스트 예산 · 압축 규약 |
| **세션 간** 정확 재사용 | 212,055자 · 문단 **125개**가 2개 이상 세션에 등장 | 상시 지시가 설치되지 않음 | `CLAUDE.md` · 룰로 이관 |

세션 간 상위 문단은 그대로 승격 후보다 — 56 · 56 · 48 · 48 · 43개 세션에 **글자 그대로** 등장한
문단 다섯이 이 코퍼스의 최상위다. 백분율은 파생 요약이고, 원장이 담는 것은 문단 원문 대신
정규화 해시 · 길이 · 등장 세션 수 · 인용이다(§10).

**"너무 단순"도 마찬가지다.** 10자 이하 19.5%는 그 자체로 결함이 아니다 — 결함은
**되물음이나 중단 개입이 뒤따른 짧은 프롬프트**다. 짧음 × 나쁜 결과만 센다.

여기서 나오는 사분면이 진단의 뼈대다:

| | 새 정보 많음 | 재첨부 위주 |
|---|---|---|
| **구체 앵커 있음** | 좋은 명세 (341건) | 앵커는 있으나 예산 낭비 |
| **앵커 없음** | 컨텍스트 의존 — 결과로만 판정 | **장황하고 모호 (637건)** — 최악 사분면 |

### 3.1.2 개입은 세 갈래로 갈린다

중단은 독립 레코드(`[Request interrupted by user]`, `origin: null`)이므로
**직전 어시스턴트 턴(무엇을 하던 중) → 중단 → 직후 사람 턴(대신 뭐라 했나)** 삼중항이 복원된다.
실측: 중단 247건 중 **243건(98.4%)** 에서 직후 지시가 확보된다.

- **중단당한 도구는 `Bash` 87건이 압도적** (다음이 Skill 4 · Read 4). 긴 명령이 개입의 주 트리거다.
  → 처방은 프롬프트가 아니라 실행 규약(타임아웃 · 백그라운드 · 진행 보고)이다.
- 개입 지시 길이 p50 88자 / p90 647자, 20자 이하 11%.

세 갈래, 전부 결정 가능한 특징으로 나뉜다:

| 갈래 | 판별 | 실측 · 뜻 |
|---|---|---|
| **무가치 재개** | 중단 전후의 touched-file 집합과 도구 시퀀스가 동일 (단어 목록이 아니라 궤적으로 판별 — §7.1이 금지한 형태를 스스로 쓰지 않는다) | 이 규칙으로 `계속 진행해` 12건이 **발견된다**. 그 문구가 규칙을 정의하지 않는다. 중단 자체가 순비용 |
| **일회성 방향 전환** | 새 경로 · 식별자 도입, 도구/파일 집합 분기 | 정상적인 스티어링 |
| **반복 운영 개입** | 같은 종류의 개입이 반복 | `워크트리도 제거하고 메인에서 진행해` · `main에 ff 머지하고 브랜치 제거` · `브랜치 없이 main에서 직접 작업해` — **규약이 설치되지 않아 매번 손으로 조종하는 것. 최고가치 발견 유형** |

### 3.1.3 반복은 두 종류이고 처방이 정반대다

- **세션 내 재진술** (같은 세션의 다른 제출에서 같은 문단) → 지시가 컨텍스트에서 밀렸다 → 프롬프트 · 컨텍스트 문제
- **세션 간 반복** → 상시 지시가 설치되지 않았다 → 하네스 문제 (`CLAUDE.md` · 룰)

하나의 반복률로 합치면 두 처방이 섞여 둘 다 틀린다.

### 3.1.4 `post_commit_reedit` — 회귀라고 부르지 않고, 에피소드도 쓰지 않는다

오라클이 내는 이름은 `post_commit_reedit`이고, 이것이 회귀인지는 스킬이 판정한다 —
파서는 판정하지 않는다는 §7.1의 규칙은 지표 **이름**에도 적용된다. 인과어를 봉투에 넣으면
판정이 사실로 굳는다.

재편집률 자체는 결함 지표가 **아니다** — 표본에서 전체 편집의 65.1%가 재편집인데, 그건 반복
편집이라는 정상 작업의 모양일 뿐이다. 회귀는 **완료 선언 이후의 재편집**이다.

트랜스크립트에 기록된 커밋 1,885건, **커밋 후 같은 파일 재편집 5,004건 / 795개 파일**.
다만 나중 커밋 이후의 후속 작업과 구분해야 하므로 **커밋 후 *다음 커밋 전*의 재편집**만
회귀로 센다.

여기서 기계 신호와 사람 신호가 같은 결함을 가리킨다: `모든 작업이 완벽하게 완료된거야?` 가
**108개 세션**에서 반복된다(§4.5). 커밋 후 재편집은 그 질문의 기계 측 대응물이고, 둘이 겹치는
지점이 *증거 없이 닫히는 Stop 게이트*라는 하나의 진단으로 수렴한다.

### 3.1.5 지표 패널 — 합성 점수는 없다

"AI 활용 점수" 같은 단일 합성 지표는 만들지 않는다. 의미가 없고, 조작 가능하며, DORA가 정확히 그
실패를 기록해 뒀다(자가보고 +20% / 실측 −19%). 각 지표는 **하나의 원인 후보와 하나의 처방**에
연결되고, 연결되지 않는 지표는 패널에 넣지 않는다.

| 지표 | 무엇을 말하나 | 방향 | 처방 |
|---|---|---|---|
| 자기 재첨부율 | 상시 지시가 프롬프트 예산을 먹고 있나 | ↓ | `CLAUDE.md` · 룰로 이관 |
| 되물음률 | 지시가 명세 부족이었나 | ↓ | 구체 앵커 · 성공 기준 |
| 무가치 개입 / 제출 | 끊어놓고 그대로 재개했나 | ↓ | 실행 규약 (타임아웃 · 진행 보고) |
| 반복 운영 개입 (종류별) | 규약이 없어 매번 조종하나 | ↓ | 워크플로우 규약 설치 |
| 세션 내 재진술 | 지시가 컨텍스트에서 밀렸나 | ↓ | 프롬프트 · 컨텍스트 예산 |
| 세션 간 반복 | 상시 지시가 미설치인가 | ↓ | `/harnex extend` |
| `post_commit_reedit` | 완료 선언 후 같은 파일을 다시 고쳤나 | ↓ | 증거를 내는 Stop 게이트 |
| `user-rejected` / 제출 | 규칙이 할 일을 사람이 했나 | ↓ | `deny-shape` |
| 훅 블로킹 ms / stop · 룰 로드 바이트 | 하네스가 얼마나 붙잡고 먹었나 | ↓ | `/harnex retire` |

### 3.2 거부: 사람 간 비교를 하지 않는다

코퍼스는 업무 프로젝트를 포함해 806개 디렉터리에 걸쳐 있다. 이 지표들은 **한 사람의 시계열로만**
의미가 있다. 사람 간 비교, 팀 순위, 개인별 리더보드는 이 도구가 산출하지 않는다 — 설정으로도
불가능해야 한다. 이건 정책이 아니라 설계 제약이다: 원장은 정규화 해시·길이·분류만 담고 프롬프트
원문을 담지 않으며, baseline 산출물에는 어떤 식별자도 넣지 않는다.

### 3.3 리포트의 척추 (고정)

순서가 곧 주장이다. 지시가 먼저 오고 하네스는 원인으로 뒤에 온다.

```
1  이번 창(window)에 무슨 일이 있었나   세션 · 제출 · 커밋 수, 커버리지
2  델타 (2회차부터, 최상단)             지표별 이전 → 현재, n, 버전 범위
3  내가 어떻게 지시했나                 프롬프트 품질 = 텍스트 특징 × 접지 신호
4  내가 어디서 개입했나                 중단 삼중항, 세 갈래 분류, 무가치 개입
5  무엇을 반복했나                      세션 내 재진술 / 세션 간 반복 (처방이 정반대)
6  어디서 되돌아갔나                    post_commit_reedit
7  하네스가 한 일                       3~6의 원인 후보로서만
8  처방 (추가 / 제거, 각각 인용)        실행 가능한 diff
9  커버리지와 한계                      읽지 못한 것, 보류한 것
```

3~6이 본문이고 7은 각주다. 8번이 없는 리포트는 실패다. 9번이 없는 리포트는 증거가 아니다.

---

## 4. 증거 (측정본)

**코퍼스:** 전체 `.jsonl` 4,871개 = **세션 트랜스크립트 1,424개** + **서브에이전트 3,446개**,
프로젝트 디렉터리 **806개**; `~/.claude/history.jsonl`에 타이핑된 프롬프트 **20,448**건.
(v1의 "세션 4,846개 + 서브에이전트 3,424개"는 중복 계산 — 4.8k 안에 서브에이전트가 이미 들어 있다.
세션 수는 모든 세션당 비율의 분모다.)

표본은 별도 표기가 없는 한 200 KB를 넘는 세션 트랜스크립트 120개 무작위 추출.

### 4.1 하네스가 개입한 횟수는 셀 수 있다

```
toolDenialKind   permission-rule   810     ← 하네스가 에이전트를 막음
                 user-rejected     288     ← 사람이 에이전트를 막음
```

한 필드에 두 개의 사실이 있고 둘 다 결정 가능하다. `permission-rule`은 harnex가
`policy/profiles.rs`에서 생성한 항목이 발동한 것이고, `user-rejected`는 규칙이 했어야 할 일을
사람이 손으로 한 것 — 반복되는 명령 형태는 `permissions.deny` 후보다.

**다만 귀속의 정밀도가 양쪽에서 다르다** (§11-1 측정 결과). `permission-rule` 레코드는 도구와
전체 명령만 담고 **매치된 규칙을 담지 않으므로**, 총량과 명령 형태별 묶음까지가 한계다 — 어느
deny 규칙을 완화할지는 사람이 `settings.json`을 보고 정한다. 반대로 `user-rejected`는 네 갈래로
갈리고 그중 143건은 런타임이 복합 명령을 분해해 **필요한 조각을 직접 지목**한다. 그리고 승인 쪽
사건은 로그에 남지 않으므로 유도 가능한 처방은 `deny-shape` 하나뿐이다.

### 4.2 Stop 게이트: 6.5시간을 쓰고 0번 막았다

```
표본 stop 수                   7,349
총 블로킹 벽시계              23,389,047 ms  =  389.8 분
hookErrors                        23
preventedContinuation              0
```

| 총계 | 호출 | 평균 | 명령 |
|---|---|---|---|
| **18,685,376 ms (5.19시간)** | 7,349 | **2,543 ms** | `afplay /System/Library/Sounds/Glass.aiff &` |
| 2,037,750 ms | 10,192 | 200 ms | `bash "${CLAUDE_PROJECT_DIR}/scripts/hooks/_runner.sh" …` |
| 1,173,854 ms | 7,349 | 160 ms | `node "${CLAUDE_PLUGIN_ROOT}/scripts/stop-review-gate-hook.mjs"` |
| 559,618 ms | 351 | 1,594 ms | `${CLAUDE_PROJECT_DIR}/hooks/_stop_runner.sh check_on_stop.py` |
| 19,639 ms | 4 | 4,910 ms | `md-wisely-agent 스펙을 끝까지 구현한다. 이 조건 자체가 지시다 …` |

- 알림음 하나가 전체 Stop 훅 블로킹 시간의 **80%** — stop당 2.5초, 뒤에 `&`가 붙어 있는데도.
- `preventedContinuation`이 7,349 stop 동안 **0**. bounded retry가 조기 종료를 치료한다는
  `guard/mod.rs`의 주장은 이 코퍼스에서 뒷받침되지 않는다.
- 마지막 행은 **훅의 `command` 필드에 프롬프트가 통째로 들어간** 실제 오설정이다.

**이 세 줄이 §1의 제거 경로가 필요한 이유 전부다.** 셋 다 지워야 하는데, 지울 수단이 없다.

### 4.3 룰은 로드되고, 공짜가 아니다

표본에서 `nested_memory` 로드 3,018건. `attachment`가 `{content, displayPath, path, type}`을
담으므로 로드당 비용이 정확하다.

| 로드 | 로드당 평균 바이트 | 룰 |
|---|---|---|
| 123 | 44,526 | `aix-platform/.claude/rules/code-style.md` |
| 117 | 10,608 | `aix-platform/.claude/rules/deprecated-annotations.md` |
| 97 | 32,755 | `aix-platform/.claude/rules/testing.md` |
| 64 | **95,070** | `aix-platform/.claude/rules/observability.md` |
| 41 | 49,673 | `aix-platform/.claude/rules/tableau-observability.md` |

`observability.md`는 95 KB짜리 주입이 64번 — 약 6 MB의 컨텍스트다. 로드와 거부가 둘 다 셀 수 있게
되면 처방이 갈린다: 늘 로드되지만 거부·교정과 한 번도 겹치지 않는 룰은 **축소**, 로드됐는데도
위반된 룰은 **재작성**, 한 번도 로드되지 않은 룰은 **제거**. 정반대 처방이고 `lifecycle`의 현재
`Silent` 신호는 이 셋을 구분하지 못한다.

### 4.4 컨텍스트는 손으로 관리되고 있다

표본 압축 308회 = 수동 302 · 자동 6. 24시간 harnex 세션 하나에서 수동 압축 4회,
`preTokens` 559k–914k → `postTokens` 12.7k–15.5k, 각 125–163초,
`cumulativeDroppedTokens` **3,029,545** 누적. `history.jsonl` 전체에서 `/compact` **620회**,
`/exit` **635회**. 그 일을 하는 붙여넣기는 서로 다른 **112개 세션**에서 반복된다.

### 4.5 상시 지시가 설치되는 대신 계속 타이핑되고 있다

| 세션 | 프롬프트 | 정체 |
|---|---|---|
| **113** | `당장의 roi 가 문제가 아니야 장기적 관점으로…` | 상시 원칙 → `CLAUDE.md` |
| **112** | `/compact 다음 작업도 계속 진행하도록…` | 손으로 하는 컨텍스트 관리 → 훅 또는 룰 축소 |
| **108** | `모든 작업이 완벽하게 완료된거야? 다시 전체…` | 증거 없이 닫히는 Stop 게이트 |
| 45 | `[pasted text #1 +53 lines] [pasted text #2 +36 lines]` | 89행 정책 문서를 45개 세션에 손으로 붙여넣음 |
| 41 | `/goal 계속 완료 됐다면서 라운드 마다 이슈가…` | 3행과 같은 것, 격화 |
| 26 | `작업한 내용에 대해서 fresh context + codex 병렬 심층 리뷰…` | 워크플로우 → 스킬 |

4행이 가장 날카롭다. 89행짜리 정책 문서를 45번 손으로 붙여넣었다는 건, 끝내 쓰이지 않은
`CLAUDE.md` 섹션이 있다는 뜻이다.

### 4.6 반복률 — 갭으로 갈라야 한다

사람 턴 3,593개 중 15자 초과 2,721개, 그중 **40.2%** 가 같은 세션 안에 동일한 앞선 쌍둥이를 갖는다.
그런데 간격이 이들을 다른 현상으로 갈라놓는다:

| 간격 | 비율 | 정체 |
|---|---|---|
| 10초 미만 | **14.5%** | 큐에 넣은 한 덩어리 — 실패가 아님 |
| 10–60초 | 3.7% | 위와 같음 |
| 1–10분 | 10.5% | 한 시도 안에서의 재입력 |
| 10–60분 | 21.8% | 한 구간의 작업 후 재입력 |
| 1시간 초과 | **49.5%** | 지시가 세션을 넘기지 못함 — 승격 후보 |

09:28:43 · 09:28:47 · 09:28:50의 동일한 세 턴은 하나의 생각을 세 메시지로 친 것이고, 같은 문장이
11:47 · 14:34 · 이튿날 07:34에 다시 나타나는 것은 지시가 붙지 않은 것이다. 갭 필터가 없으면 실패
신호가 약 5분의 1 부풀려진다.

프롬프트 길이 p50/p90/p99: **60 / 619 / 3,425**자. 19.5%가 10자 이하.

### 4.7 결과 앵커

`toolUseResult.gitOperation.commit`이 `{sha, kind, branch}`를 기록한다 — 커밋 구간은 저장소에
나중에 물어볼 수 있는 SHA로 끝난다. 서브에이전트 트랜스크립트 3,446개가 `agentType`·`model`·
`spawnDepth`별 토큰을 준다. 도구 결과의 `is_error`는 큰 세션 하나에서 4.6%.

---

## 5. 측정 함정

손으로 짠 스크립트는 이것들을 매번 다시 발견하고, 타입 있는 파서는 한 번만 새긴다.
AI가 트랜스크립트를 직접 훑는 방식으로는 이 중 넷을 못 피한다 — **오라클이 필요한 이유가 여기 있다.**

| § | 함정 | 무엇이 잘못되는가 |
|---|---|---|
| 5.1 | **토큰 이중 계산 2.31배** | 어시스턴트 레코드는 스트리밍 블록마다 쪼개지고 조각마다 같은 `message.id` 아래 `usage` 전체 사본을 싣는다. 단순 합 3,983,495 → 중복 제거 1,727,027. 부풀림이 일관돼서 아무 이상도 없어 보인다. |
| 5.2 | **줄 수 vs 레코드 수 4.3배** | `history.jsonl` 레코드 20,448건이 줄 세는 도구에서 87,327로 나온다. 프롬프트에 개행이 있다. |
| 5.3 | `turn_duration`이 성기다 | 진짜 턴 단위지만 553턴 세션에 5건뿐. 벽시계는 타임스탬프 차이로, 이건 교차 검증용. `messageCount`는 누적이며 압축 후 리셋. |
| 5.4 | **`toolUseResult`가 다형** | 객체 또는 맨 문자열(한 세션에 64건). `jq`는 죽고 Rust `struct` 역직렬화는 레코드 전체를 실패시킨다. 열거형으로 모델링하고 문자열 변형을 커버리지에 계상. |
| 5.5 | **ESC 중단은 본문 텍스트에** | `toolUseResult.interrupted`는 표본 100%가 `false`인 반면 `[Request interrupted by user]`는 본문에 230회. 구조화 필드 위에 세우면 상수 0이 나오고 정상처럼 보인다. `userModified`도 536건 전부 `false`. |
| 5.6 | **프로젝트 디렉터리가 `-`로 시작** | 상대 경로가 argv에서 플래그로 파싱된다. 오류 없이 빈 출력이 나온다. 절대 정규 경로로만 연다. |
| 5.8 | **"직후 어시스턴트 응답"은 레코드 하나가 아니다** *(신규)* | 어시스턴트 턴은 스트리밍 블록마다 쪼개지므로 사람 턴 다음 레코드 하나만 보면 사고 블록이나 부분 텍스트를 읽는다. 이번 측정에서 되물음률이 **0.0%로 나왔다가**, 사람 턴 사이 전 구간을 합쳐 재집계하니 **16.2%** 가 됐다. 응답 단위는 사람 턴 경계로 잘라 `message.id` 로 묶어야 한다. |
| 5.7 | 로케일 / `origin.kind` 커버리지 | 정렬에 `LC_ALL=C` 필요(한국어에서 `illegal byte sequence`). `origin.kind` 부재는 **레코드 단위**다 — 21개 버전 중 20개가 양쪽을 갖고 기록률이 42~66%로 고르며, 세션 119개 중 60개가 10~90% 구간에 있다. 세션도 버전도 단위가 아니다. 따라서 커버리지 분모는 **런타임이 사람을 지목한 턴**이고, 지목하지 않은 턴(전체 텍스트 user 레코드의 약 45%)은 실패가 아니라 모집단 밖이다. |

**결론: 커버리지 바닥값 아래에서는 숫자 보고를 거부한다.** 위 넷은 전부 *그럴듯한* 틀린 숫자를
만들어 내는 실패 방식이다. 편향된 숫자보다 보고 거부가 낫다.

---

## 6. 연구 근거

| 출처 | 확립한 것 | 바꾼 설계 결정 |
|---|---|---|
| [Externalization in LLM Agents (2026)](https://arxiv.org/abs/2604.08224) | *harness engineering*을 메모리·스킬·프로토콜의 통합 계층으로 명명하고, **외부화 산출물이 도움이 되는지 평가하는 것은 미해결**이라고 명시 | 이 공백이 곧 기여다. harnex는 산출물을 만들지만 아무것도 측정하지 않는다 |
| [AgentAtlas (2026)](https://arxiv.org/html/2605.20530v1) | 결과 성공만으로 부족. **제어 결정 품질**(Act/Ask/Refuse/Stop/Confirm/Recover)을 분리해야 함 | 거부가 *무엇인지* 가리키는 출판된 어휘를 재사용. `permission-rule`=Refuse, `user-rejected`=Refuse였어야 할 Confirm, "계속 진행해"=Stop 실패 |
| [AutoLibra (ICLR 2026)](https://arxiv.org/abs/2505.02820) | 지표를 열린 인간 피드백에서 **유도**할 수 있고 coverage/redundancy로 검증 | 스티어링 턴이 곧 열린 피드백. 개입 택소노미를 프로젝트마다 유도하고 커버리지와 함께 보고 — 고정 목록 출하 금지 |
| [RuVerBench (2026)](https://arxiv.org/html/2606.29920) | 긴 에이전트 트랜스크립트에서 LLM-as-judge는 시끄럽고 순서·표현에 민감. 인간 라벨 보정이 유일한 방어 | 판정 필드는 일치율을 달고 나오거나 보류. §2.2의 인용 의무와 함께 AI 진단의 신뢰 장치 |
| [DORA 2025/2026](https://getdx.com/blog/dora-metrics/) | rework를 다섯 번째 지표로 추가. 자가보고 +20% / 실측 −19% | 합성 점수 금지(§3.1), 인과 표현 금지, rework를 저장소 측 결과 앵커로 |
| [OTel GenAI semconv](https://opentelemetry.io/blog/2026/genai-observability/) | `invoke_agent`/`chat`/`execute_tool` 스팬이 표준화 중, Claude Code가 네이티브 방출 | 비용은 `hatel`에 남기고 이름만 규약에 매핑 — 경쟁하지 않음 |
| [Reasoning provenance (2026)](https://arxiv.org/pdf/2603.21692) | **선형 트레이스가 아닌 단위 분절**과 **결과의 상류 지시 귀속**이 필요 | 귀속을 산출물로. 다만 분절 단위는 이 코퍼스가 정하게 뒀다 — §7.4 |

기존 Claude Code 트랜스크립트 도구(`claude-code-log`, `claude-code-transcripts`,
`claude-code-analytics`, DuckDB/PM4Py 분석 스킬)는 렌더링하거나 집계한다. 어느 것도 하네스가
*무엇인지* 모르므로 행동을 룰·훅·권한 항목에 귀속시키거나 diff를 돌려줄 수 없다. 차별점은 전부
여기에 있고, `SKILL.md`에 적혀야 한다.

---

## 7. 설계

### 7.1 계층 — 파서는 판정하지 않고, 판정자는 세지 않고, 둘 다 인용한다

| 계층 | 소유 | 절대 하지 않는 것 |
|---|---|---|
| `harness-core::session` | 파싱, 분절, 계수, 귀속, 커버리지 보고, **인용 발행** | 개입이 *유의미*했는지 판단 |
| `harness session …` (CLI) | 명령당 정확히 하나의 봉투 (헌법 II) | stdout에 산문, 인과 표현 |
| `/harnex measure` (스킬) | 택소노미 유도, 원문 딥다이브, 진단, 처방, 리포트 발행 | 오라클이 계산한 것을 다시 계산 |
| `/harnex retire` (스킬) | 증거를 만족하는 제거만 실행 | 판정만으로 제거 |

헌법 VII: 손으로 쓴 계속 진행 단어 목록(`계속`, `진행해`, `keep going`)은 금지된 형태다. 목록은
사본이고, 내일의 표현은 아무도 추가하지 않은 항목이다. 오라클은 사람 턴마다 **특징**을 낸다 —
길이, 앞선 턴의 정규화 반복인지와 **그 사이 간격**, 새 경로·식별자 도입 여부, 직전 정지로부터의
경과, 그 뒤 파일 변경 여부, 직전 거부 존재 여부. 분류는 스킬이 한다.

### 7.2 인용 계약

모든 사실 레코드가 다음을 달고 나온다:

```json
"cite": { "session": "<sessionId>", "file": "<absolute path>",
          "uuid": "<record uuid>", "ts": "<timestamp>" }
```

`harness-core::evidence`에 `session-record` 전략을 추가한다. 리포트 문장은
`[session:<sessionId>#<uuid>]` 마커를 달고, `harness evidence`가 그 레코드가 실제로 존재하고
주장과 일치하는지 검증한다. 검증에 실패한 문장은 리포트에서 *서술*로 강등된다.

이것이 AI-native 진단을 신뢰 가능하게 만드는 유일한 메커니즘이다.

### 7.3 이웃 도구와의 역할 분담

| 도구 | 소유 | 조인 키 |
|---|---|---|
| **`hatel`** | 토큰·비용·활성 시간·도구별 지연/성공·서브에이전트 귀속 — 네이티브 OTel + 훅에서, 프로젝트 라벨이 붙은 채로. 프롬프트는 *길이만* | `session.id` |
| **`harness session`** | 의도·스티어링·거부·룰 로드·압축·제출·커밋 구간 — 트랜스크립트에서, 오프라인 | `sessionId` |
| **`nodex`** | 룰·스킬을 그래프 노드로: 백링크, 대체 관계, trust, staleness | 노드 경로 ↔ `nested_memory.path` |

- `session`은 비용을 1차 산출로 삼지 **않는다**. `hatel`이 덮으면 `source: "otel"`, 아니면 §5.1을
  적용해 `source: "transcript"`. 출처가 기록에 없는 숫자는 증거가 아니다.
- 스킬은 측정한 하네스 신호를 `hatel emit`으로 등록된 Kind에 되돌려 보낸다. 원장은 하나다.
- `nodex`는 `rule_loads`·`denials`를 노드 증거로 바꿔 `trust`·`stale`·`orphans`를 참조 세기가
  아니라 관측으로 답하게 만든다.

### 7.4 단위 — 에피소드는 폐기했다

에피소드는 시간 임계로 정의될 예정이었다. 임계를 코퍼스에서 유도하려 했고, 유도할 것이 없었다.

**저자 턴 사이 갭 분포에 자연 단절이 없다.** 10–30분 26.0% → 30–45분 9.1% → 45–60분 5.0% →
1–2시간 7.8%로 매끄럽게 감쇠한다. 골짜기가 없으므로 어떤 값을 골라도 임의다.

**커밋도 작업 단위를 가르지 않는다.** 세션의 **44%만** 커밋을 갖고, 커밋으로 닫힌 구간의
**61%에 저자 제출이 0개**다. 커밋은 단위를 경계 짓는 게 아니라 단위 *안에서* 뭉친다.

둘 다 아니므로 에피소드를 주장하지 않는다. 대신 런타임이 실제로 기록한 두 가지를 쓴다.

**제출 (submission)** — `promptSource == "queued"` 는 *에이전트가 일하는 중에 제출됨*을 뜻한다.
큐 턴은 앞의 지시를 잇고, 그 외는 새 지시를 연다. 시계를 보지 않는다. 10초 이내 연속 쌍 430건 중
**79%가 queued로 끝나므로**, 갭 임계는 이 사실의 대리물이었을 뿐이다. `queue_gap_seconds` 삭제.
실측: 저자 턴 4,048 → 제출 3,198 (79%).

**커밋 구간** — 한 커밋이 실은 파일이 *다음 커밋 전에* 다시 편집됐는가. 경계가 커밋 자신이므로
임계가 없다. 이름은 `post_commit_reedit`이고 회귀라고 부르지 않는다 — 그 판정은 스킬의 몫이다.
실측: 커밋 2,267 · 커밋 직후 재편집 2,438건 / 824파일 · 나중 커밋 이후 3,328건(평범한 후속 작업).

### 7.5 CLI 표면

각 명령은 정확히 하나의 봉투를 낸다. 사람이 아니라 스킬과 CI를 위한 계약이다.

```
harness session index      [--project <p>] [--since <d>]
    발견된 세션과 커버리지: 파싱 / 미지 타입 / 파싱 불가 / 버전 범위.

harness session facts      [--project <p>] [--since <d>]
    창 하나의 사실 원장을 봉투 하나로. 섹션: prompts · interventions ·
    repeats · post_commit_reedit · denials · harness · episodes.
    모든 사실에 인용이 붙는다.

harness session baseline   save|diff --name <n>
    스냅샷과 델타. 그사이의 .claude/ + CLAUDE.md git diff 첨부.
```

명령이 셋인 이유는 §2.1이다. **CLI는 사람 표면이 아니라 스킬과 CI를 위한 계약**이므로 소비자가
하나면 명령도 하나면 된다. 이전 판본은 `prompts` / `interventions` / `regressions` / `denials` /
`harness` / `episodes` / `ledger` / `vocab` 을 각각 명령으로 뒀는데, 그건 사람이 골라 치는 것을
전제한 세분화였고 스스로 세운 원칙과 어긋났다. 전부 `facts` 봉투의 섹션이다.


### 7.6 설정

헌법 VII — 임계값도 머신 전역 경로도 소스에 두지 않는다.

```toml
[session]
roots               = ["~/.claude/projects"]   # 내장 기본값 없음
coverage_floor      = 0.95
cost_source         = "hatel"                  # "hatel" | "transcript" | "auto"  (M3)
```

### 7.7 닫힌 스키마, 타입 있는 오류, spec 스탬프

모든 산출 레코드는 `schemars::JsonSchema`를 달고 `harness export schema session`으로 나간다.
미지의 *입력* 타입은 커버리지에 계상되며 조용히 버려지지 않고, 미지의 *출력* 필드는 구조상 불가능.

```
SESSION_ROOT_UNREADABLE   SESSION_MALFORMED_RECORD   SESSION_EPISODE_UNKNOWN
SESSION_COVERAGE_BELOW_FLOOR   SESSION_BASELINE_UNKNOWN
SESSION_VOCAB_STAMP_STALE  (경고, 절대 finding 아님)
```

트랜스크립트 포맷은 문서화되어 있지 않고 움직인다 — 이 코퍼스에만 Claude Code 27개 버전
(2.1.220–2.1.246). `bridge-session`, `atis-latch`, `worktree-state`, `relocated`, `frame-link`는
모두 최근 도착. 오늘의 형태를 가정한 파서는 미래 로그를 *더 짧은* 세션으로 읽고 *더 좋은* 숫자를
낸다. `spec.rs`가 스탬프할 표면:

`type`(22종+) · `origin.kind` · `promptSource` · `system.subtype` · `attachment.type`(25종+) ·
**`toolDenialKind`**(`permission-rule`, `user-rejected`) · 의존 필드(`message.id`,
`message.usage.*`, `toolUseResult`(다형), `…gitOperation.commit.sha`, `…userModified`,
`…agent_type`, `attributionSkill`, `isSidechain`, `compactMetadata.*`,
`hookInfos[].{command,durationMs}`, `hookErrors`, `preventedContinuation`,
`attachment.{path,displayPath,content}`, `gitBranch`, `cwd`, `version`, `promptId`)

새 값이 스탬프 없이 등장하면 digest 테스트가 빌드를 실패시킨다.

---

## 8. 제거 경로 — `/harnex retire`

§1의 구조적 결함을 닫는 신규 스킬 모드. `extend`가 닫힌 동사 메뉴를 갖듯 이것도 갖는다.
차이는 **모든 동사가 오라클이 만족시켜야 하는 증거 술어를 갖는다**는 점이다.

| 동사 | 증거 술어 (오라클이 만족시켜야 함) | 대상 |
|---|---|---|
| `drop-hook <event> <matcher>` | `preventedContinuation == 0` **그리고** 창 내 블로킹 ms > 임계 | `settings.json` 훅 항목 |
| `drop-rule <path>` | `nested_memory` 로드 0회 (창 전체) | 룰 파일 + 참조 |

동사는 둘뿐이다. 이전 판본의 나머지 셋은 감사에서 잘라 냈다:

- `shrink-rule` — 바이너리는 룰의 **어디를** 자를지 모른다. 그건 제거가 아니라 편집이고 결정 가능한
  증거 술어가 없다. 축소는 리포트가 후보로 지목하고 사람이 `extend` 쪽에서 편집한다.
- `drop-skill` — 미사용 스킬의 증거가 아직 코퍼스에 없다. 증거가 나오면 그때 동사를 만든다.
- `deny-shape` — 제거가 아니라 추가다. `extend` 쪽으로 옮겼다.

**불변식 (모든 동사 공통):**

1. **판정만으로는 발동하지 않는다.** 증거 술어가 거짓이면 스킬은 제거를 거부하고 그 사실을
   말한다. AI가 "이건 필요 없어 보인다"고 생각하는 것으로는 부족하다.
2. **모든 제거는 인용을 남긴다.** `lifecycle` 결정 원장에 사람이 쓴 근거 + 오라클 인용을 함께
   기록한다. 근거 없는 제거는 없다.
3. **되돌릴 수 있는 커밋 하나.** 제거는 하나의 커밋으로 착지하고 revert 한 번으로 원복된다.
4. **관리 영역 밖은 건드리지 않는다.** harnex가 생성하지 않은 항목은 제거 대상이 아니라 보고
   대상이다 — 운영자가 직접 판단한다.
5. **커버리지 바닥값 아래면 실행하지 않는다.** 읽지 못한 로그가 많으면 "로드 0회"는 "로드된 적
   없음"이 아니라 "못 봤음"이다.

`keep-soften-cut`의 CUT은 여전히 *생성 시점 필터*를 뜻한다. 이 모드는 **retire** — 이미 놓인
것의 제거 — 이고, 두 단어를 겹쳐 쓰지 않는다. `reference/keep-soften-cut.md`에 그 구분을 명시한다.

---

## 9. 실행 계획

각 마일스톤은 **쓸 수 있는 것 하나**로 끝난다. 순서는 "결정을 바꾸는 것 먼저"이고, 앞의 둘은
이미 존재하는 추가 경로(`/harnex extend`)로 착지하므로 제거 경로를 기다리지 않는다.

### M0 — 지시 센서 + 첫 진단

- `harness-core::session` 뼈대: 절대 경로 탐색(§5.6), 다형 허용 레코드 파싱(§5.4), 사람 턴 경계로
  자른 응답 집계(§5.8), 커버리지(§5.7), **인용 발행**(§2.2)
- `harness session facts` 의 `prompts` 섹션 — 길이 · 구체 앵커(저장소 실재 검증) ·
  정확 문단 재사용 · 붙여넣기 여부 × 접지 신호(되물음 · 직후 중단 · 재입력)
- 같은 봉투의 `interventions` 섹션 — 중단 삼중항(직전 도구 → 중단 → 직후 지시)과 궤적 비교
- `/harnex measure` 최소 모드 → 아티팩트 리포트 (척추 3 · 4 · 8 · 9절)

**완료 기준:** 사용자 자신의 코퍼스에서 **`CLAUDE.md`로 옮길 문단 1개를 인용과 함께 지목**하고,
옮긴 뒤 재측정할 지표(자기 재첨부율 · 재입력률)를 명시한다. 현재 코퍼스 기준 후보는 68.2%
재첨부분의 최상위 문단이다.

**착지 (2026-08-26).** `harness-core::session` + `harness session index|facts`.
전체 코퍼스 실측: 4,908 파일 · 1,570,166 레코드 · malformed 0 · unreadable 0 · 11.8초 ·
저자성 커버리지 **97.70%** (n=4,209) · 미소비 레코드 타입 23종을 이름별로 계상.
저자 프롬프트 4,043건, 문단 문자 1,587,360, **세션 내 재사용 72.3%**, 2개 이상 세션에 반복된
문단 156개. 최상위 승격 후보는 **59개 세션 · 550회 · 119자**이며 인용 550건을 달고 나온다.

**M0에서 뺀 것 — 중단 개입.** 중단을 구조로 식별할 방법이 없다. `interruptedMessageId`는 존재하지만
전체의 약 70%만 덮고 버전 게이팅도 아니어서(19개 버전 중 14개가 양쪽) 그 부분집합을 세면 관측치가
알 수 없는 만큼 낮게, 즉 듣기 좋은 방향으로 치우친다. 나머지를 잡으려면 런타임이 찍는 리터럴을 본문에서
매칭해야 하는데, 그 문구가 바뀌는 날 "중단 0건"을 태연히 보고하게 된다. 건전한 식별이 생길 때까지
근사치를 출하하지 않는다.

### M1 — 제출, 반복 분리, 커밋 구간 재편집

- 제출 접기 — `promptSource == "queued"` 로, 시계 없이
- 세션 내 재진술 vs 세션 간 반복 분리 — 처방이 정반대이므로 하나의 비율로 합치지 않는다
- `facts` 의 `post_commit_reedit` 섹션 — 커밋 후 **다음 커밋 전** 재편집
- 세션 간 반복 → `PromotionCandidateFinder` → `/harnex extend`
- `spec` 스탬프 + digest 테스트

**완료 기준:** "113개 세션에 타이핑한 문장"이 `CLAUDE.md` 한 줄로 착지하고, "커밋 후 재편집"이
`post_commit_reedit`이 파일과 그것을 실은 커밋을 인용과 함께 짚는다.

**착지 (2026-08-26).** 제출 접기 · 반복 분리 · 커밋 구간 재편집. 에피소드는 위 §7.4의 측정으로
폐기했고, 그 결과 설정 파라미터 두 개(`episode_gap_minutes`, `queue_gap_seconds`)가 추가되기 전에
사라졌다.

전체 코퍼스 실측 (12.0초):

```
저자 턴 4,048 → 제출 3,198 (79%)
문단 문자 1,587,360
  세션 간 반복    1,353,394  (85.3%)  문단 156개  → 설치 안 됨      → CLAUDE.md / 룰
  세션 내 재진술     27,333  ( 1.7%)  문단  89개  → 컨텍스트에서 밀림 → 프롬프트 예산
커밋 2,267 · 커밋 직후 재편집 2,438건/824파일 · 나중 커밋 이후 3,328건
```

**두 반복률의 비가 50:1이다.** 하나로 합쳤다면 거의 항상 틀린 처방이 나왔을 것이므로, 분리해야
한다는 §3.1.3의 주장은 실측으로 뒷받침된다. 세션 내 재진술 상위는 한 세션 안에서 16개 제출에 걸쳐
다시 쓰인 184자 설계 제약이다 — 컨텍스트에서 밀려난 것이지 설치가 안 된 것이 아니다.

### M2 — 하네스 측과 제거 경로

- `facts` 의 `denials` · `harness` 섹션 — 3~6절 발견의 **원인 후보**로서
- `/harnex retire` 닫힌 동사 메뉴 + 증거 술어 + 다섯 불변식(§8)
- `evidence`에 `session-record` 전략 추가 → 리포트 문장 검증
- `lifecycle` 결정 원장 배선, `keep-soften-cut.md`에 CUT ↔ retire 구분 명시

**완료 기준:** `preventedContinuation: 0`인 Stop 훅이 증거 → 결정 원장 → 되돌릴 수 있는 커밋으로
제거된다. **여기서 래칫이 풀린다.**

### M3 — 플라이휠 폐쇄

- `baseline save|diff` + 그사이 `.claude/` diff 첨부, 리포트 2회차부터 델타 블록 최상단
- 예약 재측정 — 델타가 임계를 넘을 때만 리포트를 연다
- `nodex` 노드 증거, `hatel emit` 배선
- **여기서 처음으로 판정 필드가 나온다** — 유도된 개입 택소노미(coverage · redundancy)와
  인간 라벨 대비 일치율. M0~M2 의 리포트는 결정 가능한 사실과 인용된 후보만으로 성립하므로
  그때까지 스킬은 판정 필드를 내지 않는다. 보정 없는 판정을 먼저 내보내면 그것이 사실로 굳는다.

**완료 기준:** 2회차 리포트가 델타를 `n`·버전 범위와 함께 보여준다.

```
세션 간 반복 문자          85.3%  → 24.1%   (n=3,198 제출)
되물음률                  16.2%  →  9.4%
세션 내 재진술 문자          1.7%  →  0.6%
세션 간 반복(상위 문장)    113회  →   0회   (CLAUDE.md 로 이관됨)
커밋 후 다음 커밋 전 재편집   1.08 →  0.4
그사이 하네스 커밋 4건 (CLAUDE.md +1문단, rules/git-workflow.md 신규, hooks.json -1, …)
```

---

## 9.5 엣지 케이스 — 실측으로 확인한 것

설계가 아니라 코퍼스에서 확인했다. 확인되지 않은 것은 적지 않았다.

| 경우 | 실측 | 처리 |
|---|---|---|
| `promptSource == "suggestion_accepted"` | **63건** | 사용자가 쓴 것이 아니다. 프롬프트 통계에서 제외 |
| `promptSource == "queued"` | 728건 (20.0%) | 사람이 쓴 것이 맞다. 제출 단위로 접는다(§7.4) |
| `isSidechain` 인 사람 턴 | **0건** (3,634 전부 false) | 서브에이전트 오염 없음. 확인된 음성 결과 |
| `origin.kind` 이 `task-notification` · `peer` | 1,333 · 447건 | 사람 프롬프트가 아니다. 제외하고 커버리지에 계상 |
| 로그가 없는 신규 사용자 | — | 커버리지 0 → 숫자 대신 "데이터 없음". 0%를 내면 안 된다 |
| 루트를 열 수 없음 | — | `SESSION_ROOT_UNREADABLE`. 커버리지 0으로 성공 보고하면 안 된다 |

마지막 행은 이번에 직접 밟았다. 검증 스크립트가 상대 경로 때문에 파일을 하나도 열지 못했는데
광범위한 예외 처리가 그것을 삼키고 **자신 있는 0%를 출력**했다. §5의 "그럴듯한 틀린 숫자"가
오라클을 쓰지 않는 쪽에서 그대로 재현된 사례이고, 열기 실패를 커버리지가 아니라 타입 오류로
올려야 하는 이유다.

---

## 10. 한계와 거부

- **네트워크 없음.** 헌법 I. 모든 입력은 이미 디스크에 있는 로컬 파일이다.
- **관측이지 실험이 아니다.** 과제 난이도·모델 버전·Claude Code 버전이 모두 밑에서 움직인다.
  모든 델타는 `n`과 양쪽 버전 범위를 달고 나오고, 설정된 `n` 아래에서는 숫자만 보고하고
  **비교는 보류**한다. 봉투에는 인과를 뜻하는 단어가 들어가지 않는다.
- **품질 판정이 아니다.** 오라클은 공정 사실과 저장소 결과를 보고한다. 작업이 *좋았는지*는
  측정이 아니라 리뷰의 몫이다.
- **합성 점수 없음, 사람 간 비교 없음.** §3.1, §3.2. 설계 제약이지 정책이 아니다.
- **오라클 없이는 숫자 없음.** `harness` 바이너리가 없으면 스킬은 정성 딥다이브만 하고 수치는
  내지 않는다. §5의 넷은 AI가 로그를 직접 훑는 방식으로는 피할 수 없는 함정이다.
- **판정은 보정 없이 보류.** 판정 필드는 `value (κ=…, n=…)`로 찍히거나 나오지 않는다.
- **원문을 저장하지 않는다.** 원장은 정규화 해시·길이·분류만 담는다. 원문은 명시적 플래그로만,
  baseline 산출물에는 절대 넣지 않는다. `hatel`이 이미 같은 선택을 했으므로 두 원장이 나란히
  놓이려면 선택이 아니라 필수다.
- **쓰기는 `path_guard`로만.** 헌법 III. 네트워크 싱크에는 아무것도 닿지 않는다. 외부 공개는
  운영자의 의도적 행위여야 하며 명령 실행의 부수 효과일 수 없다.

---

## 11. 미해결 질문

1. ~~**거부를 특정 규칙에 귀속시킬 수 있는가?**~~ — **해결됨 (2026-08-26 측정).** 불가능하고,
   시도해서도 안 된다. 레코드 문구는 `Permission to use <Tool> with command <전체 명령> has been
   denied.` 뿐이고 매치된 규칙 항목이 없다. `policy::rule`에는 매처가 없다 — `parse`(도구/지정자
   분리), `effect`(consulted vs inert), `bash_base`(꼬리 와일드카드 정규화)는 전부 **선언 측**
   문법이며, "이 규칙이 이 명령에 걸리는가"를 답하는 **평가 측** 함수는 존재하지 않는다. 만들면
   Claude Code 권한 엔진의 매칭 의미를 재구현하는 것이고, 그건 `spec.rs`가 존재 이유로 삼는 바로
   그 드리프트다 — 비교의 양쪽이 다 우리 것이 되어 조용히 어긋나고, 어긋난 "이 규칙이 N번 걸렸다"가
   삭제 결정의 입력이 된다. 대신 `permission-rule` 거부는 **총량 + 명령 형태별 묶음**으로만
   보고하고, 어느 deny 규칙을 완화할지는 사람이 `settings.json`을 보고 정한다. 리포트가 그 한계를
   명시한다.

   그 과정에서 두 가지가 더 나왔다. **(a) `user-rejected`는 한 덩어리가 아니다** — 120세션 341건이
   네 갈래로 갈린다: 복합 명령 분해로 조각을 지목한 것 **143**(`The following part requires
   approval: <서브커맨드>` — `echo` 29 · `mkdir` 17 · `python3` 13 · `lore` 13 · `cd` 13 · `xargs` 9),
   확장/패턴 가드 **38**(`Contains simple_expansion` 21 · `shell` 8 · `command_substitution` 4 ·
   `brace` 3 · `while_statement` 2), 단일 명령 승인 필요 **37**, 사람이 명시적으로 거부한 것 **32**.
   서브커맨드 지목은 **프롬프트가 뜬 이유**이지 **사람이 거부한 이유**가 아니다 — 오라클은 둘을
   별개 특징으로 내보내고 스킬은 둘을 융합하면 안 된다.
   **(b) 승인 쪽 신호는 로그에 없다.** `attachment.type == "command_permissions"`는
   `{allowedTools: []}` 스냅샷일 뿐 결정 로그가 아니고(120세션 206건, 전부 같은 두 키), 거부 없이
   남은 "requires approval" 문자열은 341건 대비 **6건**뿐이다. 즉 *마찰은 있었지만 승인된* 사건은
   관측 불가능하다. 따라서 `allow` 후보를 유도하는 동사는 **만들지 않는다** — 근거가 없는데 있는
   척하게 된다. 거부만 관측되므로 유도 가능한 처방은 `deny-shape` 하나다.

2. ~~**에피소드 간격 기본값.**~~ — **해결됨 (2026-08-26 측정).** 유도할 것이 없었다. 갭 분포에
   자연 단절이 없고(10–30분 26.0% → 30–45분 9.1% → 45–60분 5.0% → 1–2시간 7.8%), 커밋도 단위를
   가르지 않는다(세션의 44%만 커밋 보유, 커밋 구간의 61%에 저자 제출 0개). 에피소드를 폐기하고
   제출(`queued`)과 커밋 구간으로 대체했다 — §7.4. 파라미터 두 개가 추가되기 전에 사라졌다.
3. **로드됐는데 위반됨 조인은 어디서?** 룰 내용(harnex), 룰 로드(이 모듈), 결함 증거(`audit`
   finding · 거부 · 저장소 rework)가 모두 필요하다. `audit`이 `session`을 소비하는 형태가
   유력하나 실제 설계 판단이 필요하다.
4. **판정자 보정 세트.** 크기, 라벨링 주체, 저장 위치. 프로젝트마다 다르므로 플러그인에 담을 수
   없고 baseline 산출물 옆이 유력하다. 사용자가 리포트에서 판정을 수정하면 그것이 보정 라벨이
   되는 경로가 자연스럽다 — 설계할 가치가 있다.
5. **탐색 vs `transcript_path`.** `~/.claude/projects` 탐색은 그 외에는
   `${CLAUDE_PROJECT_DIR}`만 건드리는 바이너리에 머신 전역 경로를 들이는 일이다. 권고는 내장
   기본값 없는 설정 선언 `roots`. 훅이 주는 `transcript_path`는 라이브 세션 경로로 남는다.
6. **`repeat_normalize` 재사용.** `lifecycle`의 함수여야 한다고 §7.6이 주장한다. 두 정규화의
   의도가 동일한지 확인 후 공유할 것. 두 의미로 쓰이는 공유 함수는 두 개의 함수보다 나쁘다.
7. ~~**파싱 예산.**~~ — **해결됨 (2026-08-26 실측).** 전체 코퍼스 4,908 파일 · 1,570,166 레코드를 릴리스 빌드가 **11.8초**에 읽는다(단일 스레드, malformed 0, unreadable 0). `rayon`은 필요 없고, 필요를 만든 측정이 나오기 전에는 넣지 않는다.

---

## 부록: 측정 재현

모든 수치는 2026-08-26에 `~/.claude/projects/**/*.jsonl`과 `~/.claude/history.jsonl`에 대해
`jq`로 얻었다. 전 구간 절대 경로 — §5.6.

```bash
ROOT="$HOME/.claude/projects"
find "$ROOT" -maxdepth 2 -name '*.jsonl' -not -path '*/subagents/*' -size +200k \
  | sort -R | head -120 > /tmp/s120.txt

# §4.1 — 거부 원장
while read f; do jq -r 'select(.toolDenialKind!=null)|.toolDenialKind' "$f"; done < /tmp/s120.txt \
  | sort | uniq -c | sort -rn

# §4.2 — 명령별 Stop 훅 비용, 그리고 실제로 무언가를 막은 적이 있는지
while read f; do
  jq -r 'select(.type=="system" and .subtype=="stop_hook_summary")|.hookInfos[]?
         |[(.durationMs//0|tostring),(.command|gsub("\\s+";" ")|.[0:70])]|@tsv' "$f"
done < /tmp/s120.txt \
  | awk -F'\t' '{n[$2]++;s[$2]+=$1} END{for(k in s) printf "%12.0f ms %6d %s\n",s[k],n[k],k}' | sort -rn

# §4.3 — 룰 로드와 바이트 비용
while read f; do
  jq -r 'select(.type=="attachment" and .attachment.type=="nested_memory")
         |[.attachment.path,((.attachment|tostring|length)|tostring)]|@tsv' "$f"
done < /tmp/s120.txt \
  | awk -F'\t' '{c[$1]++;b[$1]+=$2} END{for(k in c) printf "%6d %9.0f %s\n",c[k],b[k]/c[k],k}' | sort -rn

# §5.1 — 2.31배 토큰 함정
F="<session>.jsonl"
jq -r 'select(.type=="assistant")|.message.usage.output_tokens//0' "$F" | awk '{s+=$1} END{print "naive:",s}'
jq -r 'select(.type=="assistant" and .message.id!=null)
       |[.message.id,(.message.usage.output_tokens//0|tostring)]|@tsv' "$F" \
  | sort -u -k1,1 | awk -F'\t' '{s+=$2} END{print "deduped:",s}'

# §5.4 — 다형 필드. 이 가드가 없으면 위 패스가 조용히 죽는다
jq -r 'select((.toolUseResult|type)=="object")|.toolUseResult.interrupted//empty' "$F"
```
